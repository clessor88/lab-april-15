function sum(x,y){
  var result = x + y;
  console.log('"The sum of ' + x + ' and ' + y + ' is ' + result + '."');
  return x + y;
}

function multiply(x,y){
  var result = x * y;
  return console.log('"The product of ' + x + ' and ' + y + ' is ' + result + '."');
}
